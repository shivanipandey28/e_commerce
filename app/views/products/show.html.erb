<!DOCTYPE html>
<html>
<head>
  <title>Product Details | My E-commerce Website</title>
  <!-- Include Bootstrap CSS -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
  <style>
    /* Additional Custom Styles */
    body {
      padding: 20px;
    }

    .actions {
      margin-top: 20px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Product Details</h1>
    <div class="row">
      <div class="col-md-6">
        <h4><%= @product.name %></h4>
        <p>Brand: <%= @product.brand_name %></p>
        <p>Description: <%= @product.description %></p>
        <p>Price: <%= @product.price %></p>
        <p>Rating: <%= @product.rating %></p>
        <p>Status: <%= @product.status %></p>
        <p>Quantity: <%= @product.quantity %></p>
        <% if current_user.role =="buyer" %>
          <%= link_to "Buy",new_order_path,  class: "btn btn-primary" %>
          <%= link_to "Add to Cart" %>
        <% end %>
      </div>
    </div>

    <div class="actions">
      <ul class="list-inline">
        <% if current_user && current_user.role == "seller" %>
          <li class="list-inline-item"><%= link_to "Edit", edit_product_path(@product), class: "btn btn-primary" %></li>
          <li class="list-inline-item"><%= link_to "Delete", product_path(@product), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "btn btn-danger" %></li>
          <li class="list-inline-item"><%= link_to "Create New", new_product_path, class: "btn btn-success" %></li>
        <% end %>
        <li class="list-inline-item"><%= link_to "Back", products_path(@products), class: "btn btn-secondary" %></li>
      </ul>
    </div>
  </div>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
</body>
</html>
